<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Spot</title>
  {{!-- Tailwind CSS --}}
  <script src="https://cdn.tailwindcss.com"></script>  
  <link rel="stylesheet" href="css/style.css">
</head>  
<body class="text-slate-800 bg-state-50">  
  <div id="wrapper" class="max-w-screen-xl mx-auto px-4">
    <h1>Logged in {{logged_in}}, as {{session_user_name}}</h1>
    <header>
      <nav class="flex flex-wrap items-center justify-between py-4">
      <ul class="flex gap-x-4">
        <li class="py-3 text-2xl font-bold hover:text-emerald-400 transition duration-300"><a href="/">Home</a></li>              
        <li class="py-3 text-2xl font-bold hover:text-emerald-400 transition duration-300"><a href="/dashboard">Dashboard</a></li>        
      </ul>
      {{!-- Display login/logout link depending on wheter or not the user is signed in --}}
      {{#if logged_in}}
        <button id="logout" class="py-3 text-2xl font-bold hover:text-emerald-400 transition duration-300">Logout</button>        
      {{else}}
        <a href="/login"
          class="py-3 text-2xl font-bold hover:text-emerald-400 transition duration-300" >Log in / Register</a>
      {{/if}}
    </nav>
    </header>
    {{{ body }}}
    <footer>
      <!-- Copyright section -->
      <!-- Source -->
      <!-- https://stackoverflow.com/questions/6002254/get-the-current-year-in-javascript -->
      <p class="text-center py-4">Copyright © Mirek Nyzio <script>document.write( new Date().getFullYear() );</script></p>
    </footer>
  </div>

  {{!-- Allow additional functionality for logged in users --}}
  {{#if logged_in}}
    <script src="/js/logout.js"></script>
  {{/if
  }}
</body> 
</html>
