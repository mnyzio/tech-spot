<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Spot</title>
  {{!-- Tailwind CSS --}}
  <script src="https://cdn.tailwindcss.com"></script>  
  <link rel="stylesheet" href="css/style.css">
</head>  
<body class="text-slate-800 bg-state-50">  
  <div id="wrapper" class="max-w-screen-xl mx-auto px-4">
    {{!-- <h1>Logged in {{logged_in}}, as {{session_user_name}}</h1> --}}
    <header>
      {{!-- Hamburger menu --}}
      <button class="hamburger block sm:hidden pt-4" 
            id="hamburger-btn">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
        </svg>  
      </button>
      <nav id="nav-list" class="hidden sm:flex sm:flex-row sm:flex-wrap sm:items-center sm:justify-between sm:py-4">
      <ul class="sm:flex sm:flex-row sm:gap-x-4">
        <li class="p-1 sm:p-3 text-xl font-bold hover:text-emerald-400 transition duration-300"><a href="/">Home</a></li>              
        <li class="p-1 sm:p-3 text-xl font-bold hover:text-emerald-400 transition duration-300"><a href="/dashboard">Dashboard</a></li>        
      </ul>      
      {{!-- Display login/logout link depending on wheter or not the user is signed in --}}
      {{#if logged_in}}
        <button id="logout" class="p-1 sm:p-3 text-xl font-bold hover:text-emerald-400 transition duration-300">Logout</button>        
      {{else}}
        <a href="/login"
          class="p-1 block sm:p-3 text-xl font-bold hover:text-emerald-400 transition duration-300" >Log in / Register</a>
      {{/if}}
      </nav>    
    </header>
    {{{ body }}}
    <footer>
      <!-- Copyright section -->
      <!-- Source -->
      <!-- https://stackoverflow.com/questions/6002254/get-the-current-year-in-javascript -->
      <p class="text-center py-4">Copyright © Mirek Nyzio <script>document.write( new Date().getFullYear() );</script></p>
    </footer>
  </div>

  {{!-- Hamburger menu --}}
  <script src="/js/menu.js"></script>

  {{!-- Allow additional functionality for logged in users --}}  
  {{#if logged_in}}
    <script src="/js/logout.js"></script>
  {{/if
  }}
</body> 
</html>
